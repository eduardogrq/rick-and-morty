import {createSlice} from '@reduxjs/toolkit'

export const challengesSlice = createSlice({
    name: 'challenges',
    initialState: ['desde challenges', 'primer estado'],
    reducers: {

    }
})

export default challengesSlice.reducer;